import 'package:flutter/material.dart';

import 'app.dart';

class MotionAnimationsReplyUI extends StatelessWidget {
  const MotionAnimationsReplyUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const ReplyApp();
  }
}
